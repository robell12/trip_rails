<h1><%= @trip.title %></h1>

<%= link_to 'Edit', edit_trip_path(@trip) %>
<%= link_to 'Delete', trip_path(@trip), method: :delete, data: {confirm: 'You sure?'} %>

<div>
	<p>
		This trip is during: <strong><%= @trip.time %></strong>
	</p>
</div>

<%= link_to "#{@trip.title.pluralize} Location", trip_locations_path(@trip) %>